{
  "name": "agentlinter-demo-workspace",
  "version": "0.3.1",
  "private": true,
  "scripts": {
    "build": "tsc -p .",
    "test": "vitest run",
    "lint": "eslint .",
    "export:report": "node scripts/export-report.js"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0"
  }
}
