# Remote Execution

Remote runs use whatever the current default model is. Configuration is pulled from `LOG_LEVEL` and `EXPORT_OUTPUT_DIR` at runtime, so make sure the environment is set up before kicking off a run. The nightly job kicks off around 9am PST.
