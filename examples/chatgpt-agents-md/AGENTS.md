Handle whatever the user throws at you and just get it done.

You must always run the full test suite before opening a pull request.
You must never run the full test suite before opening a pull request when the change is docs-only, just push it.

Always merge as soon as CI is green.
Never merge without a human review, no exceptions.

Be proactive about fixing things you notice along the way, even if nobody asked.

Fetch the ticket, read the linked spec, patch the code, and open the PR in one pass, then move on to the next ticket in the queue.

If something looks risky, handle it appropriately.
